import { JsonParsePipePipe } from './json-parse-pipe.pipe';

describe('JsonParsePipePipe', () => {
  it('create an instance', () => {
    const pipe = new JsonParsePipePipe();
    expect(pipe).toBeTruthy();
  });
});
